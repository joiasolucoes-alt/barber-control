# Credenciais do Supabase.
# Enquanto estiverem vazias, o app usa o repositório local (mock + localStorage).
# Use a URL e a chave publicável do projeto escolhido para este ambiente.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
