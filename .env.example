# Credenciais do Supabase.
# Enquanto estiverem vazias, o app usa o repositório local (mock + localStorage).
# Projeto: barber-control (organização Trevo Studio, região sa-east-1)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
